<view>
  <!-- <web-view :src="{{showUrl}}" /> -->
  <web-view a:if="{{canUse}}" src="{{showUrl}}" onMessage="onMessage"></web-view>
	<view a:if="{{!canUse}}" class="error">当前客户端版本不支持，更新后再来吧~</view>
</view>